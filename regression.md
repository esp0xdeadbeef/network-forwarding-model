- state=solved | target=fs981-file-reachability-runner-gate | evidence=2026-06-02 Wave 2 verifier reran `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=6 bash tests/test.sh` after splitting active route implementation modules and fixing executable metadata. The runner now passes `test-nix-file-loc.sh`, `test-no-parent-relative-imports.sh`, and the previously non-executable focused tests through the broad runner; it fails only the pre-existing `overlay-semantic-stage-budget` FS-940 unresolved regression row. Focused guards also passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-requester-lane-routes.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-internal-route-equivalence-contract.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-aggregation-none-preserves-internal-prefixes.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-interface-no-peer-null-duplicates.sh`. | expected=Active Nix implementation files stay below the repository ownership-split guard, broad runners can execute every listed focused test directly, and route behavior remains unchanged. | owner=network-forwarding-model | reason=Route normalization, remote-prefix facts, and site-route planning were split into ownership modules below 200 lines; four listed focused tests now carry executable metadata.

- state=solved | target=dns-service-requester-lane-reachability | evidence=2026-06-02 focused NFM construction test `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-requester-lane-routes.sh` passes for locked `network-labs/examples/s-router-overlay-dns-lane-policy/intent.nix`. It proves `s-router-upstream-selector` emits `10.20.10.0/24` and `fd42:dead:beef:0010:0000:0000:0000:0000/64` on `p2p-s-router-policy-only-s-router-upstream-selector--access-s-router-access-client--uplink-east-west` with `lane.access=s-router-access-client` and `lane.uplink=east-west`, and does not scope those DNS service routes to denied DMZ/streaming lanes. Related checks passed: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-source-scope-contract.sh`, `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-hostile-dns-east-west.sh`, and `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=4 bash tests/test-network-labs-output.sh`. | expected=For a modeled recursive DNS service relation, NFM emits requester-lane reachability to the service provider prefix for each allowed requester/uplink scope and does not require CPM or renderers to clone DNS routes from a provider lane. | owner=network-forwarding-model | reason=NFM now derives DNS service requester scopes from compiler traffic paths, selects the scoped lane during internal route planning, and carries structured lane metadata on the emitted route. CPM/renderers must still rerun their FS-540 integration checks before SIT/HAT/SAT promotion.

- state=not-solved | target=overlay-semantic-stage-budget | evidence=2026-06-02 `benchmarks/overlay-semantic-eval.sh` times NFM from precomputed compiler JSON and records upstream compiler cardinality plus downstream route expansion. Current default 3000 ms stage budget fails: `s-router-overlay-dns-lane-policy` 10321 ms with max_relations=13, max_paths=13, max_routes=1350; `tri-site-dual-wan-overlay-integration-static` 10209 ms with max_relations=20, max_paths=20, max_routes=1110; `tri-site-s-router-overlay-egress` 9708 ms with max_relations=20, max_paths=20, max_routes=1268. Route-shape for `s-router-overlay-dns-lane-policy` showed 2149 routes total: 1420 internal-reachability, 366 overlay-reachability, 212 connected-reachability, 142 default-reachability, 9 runtime-routed-prefix-return; only 4 exact duplicate route keys were found. Skip diagnostics showed internal route expansion owns most cost; with the temporary raw internal append probe the benchmark dropped to about 4.3-5.0s but route expansion increased, so repeated normalization is a hotspot but raw append is not a correct fix. Follow-up probes on 2026-06-02 tested cached real-route graphs, cached non-overlay-transit graphs for nearest defaults, builtins-based route normalization, direct group materialization, p2p CIDR summarization, and exact internal-route merge. The route-output and return-route guards stayed green for cache/normalization/direct-materialization probes, but the benchmark still failed around 8.2-10.5s; p2p summarization reduced max_routes to 1050/908/974 and kept focused return-route checks green but made timing worse around 10.7-12.6s, so route count alone is not the bottleneck. The exact internal-route merge probe reduced timing toward 3.4-4.5s but inflated route cardinality across nearly every network-labs example, so it is rejected and not retained. Nix stats for `s-router-overlay-dns-lane-policy` showed full eval at about 9.7s, 9.26M function calls, 10.5M thunks, and 287k list concats; after preserving exact destinations under `aggregation.mode = "none"`, full eval improved to about 8.7s, 8.03M function calls, 9.18M thunks, and 250k list concats. Focused construction proof `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-aggregation-none-preserves-internal-prefixes.sh` passes and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-network-labs-output.sh` passes after the narrow expected update for two `multi-wan` IPv6 internal route counts. The H2 profile-hypothesis test passed and isolated the layer: `full_ms=8497`, `skip_internal_ms=1475`, `delta_ms=7022`, `full_routes=2149`, `skip_internal_routes=826`, `delta_routes=1323`. A grouped-by-link materializer probe preserved route outputs but did not solve H2: focused profile remained `full_ms=8324`, `skip_internal_ms=1259`, `delta_ms=7065`, and the benchmark still failed at `10216/7753/8183 ms`. A scalar route-key probe preserved focused checks after handling structured `lane` scope, but did not improve H2 (`full_ms=8281`, `skip_internal_ms=1169`, `delta_ms=7112`), so it is rejected and not retained. Manual output audits found and fixed correctness bugs while FS-940 remained open: duplicate peerSite-null overlay routes, overlay-interface defaults misclassified as overlay reachability, and external-ingress defaults attached to a source overlay p2p link while using a target WAN p2p peer. Focused guards now include `test-overlay-interface-no-peer-null-duplicates.sh`, `test-external-ingress-uplink-defaults.sh`, and `test-p2p-route-next-hop-peer-contract.sh`; overlay-underlay compatibility checks still pass. Clean accepted-patch benchmark still fails: `s-router-overlay-dns-lane-policy` 9150 ms, `tri-site-dual-wan-overlay-integration-static` 8880 ms, and `tri-site-s-router-overlay-egress` 8758 ms. | expected=The NFM semantic stage must leave budget for CPM and renderer semantic evaluation instead of consuming the full FS-940 end-to-end objective. Benchmark output must classify whether slowness is caused by abnormal upstream cardinality or by current-stage route expansion/evaluator cost while preserving route-class and p2p next-hop correctness. | owner=network-forwarding-model | reason=Compiler provider output is bounded for the measured fixtures, so current evidence points to NFM internal route-expansion/evaluator cost plus secondary nearest-default route planning cost. The next fix should continue constructing the SMS shared route group planner or equivalent site-level route materialization plan, not hide the failure in renderer, SAT, or benchmark threshold changes.

- state=solved | target=semantic-lib-imports-full-nixpkgs-package-set | evidence=2026-06-02 Nix eval profiling of the overlay example render path showed `network-renderer-nixos` `buildForBox` spent about 15s before NixOS option evaluation. The call graph entered `network-forwarding-model` through `flake.libBySystem.*`, then forced `flake.nix:mkPkgs` and Nixpkgs/stdenv frames even though semantic forwarding construction only needed library functions. `flake.nix` now constructs `applyForwardingModel` with `nixpkgs.lib // { network = nixpkgs-network.lib.network; }` and imports `pkgs` only inside `writeJSON` and `writeFromCompilerInputPath` package-producing helpers. Focused guard: `tests/test-semantic-lib-performance-boundary.sh` rejects reintroducing `pkgs.lib` on the semantic lib path. | expected=FS-940 semantic compile/evaluation paths shall not import a full Nixpkgs package set or force stdenv just to obtain library functions. Package-producing helpers may still import `pkgs` when writing derivations. | owner=network-forwarding-model | reason=This solved one dependency-forcing bug, but it does not solve the separate overlay semantic stage-budget failure.

- state=solved | target=overlay-core-local-hostile-return-routes | evidence=2026-05-28 live FAT debugging proved hostile DNS replies reached the local overlay edge and then looped on `nixos-router-core-nebula` `overlay-west` because table-selected return routing lacked the local hostile prefixes via the real `core-nebula -> upstream` p2p leg. Artifact split showed NFM omitted `10.20.70.0/24`, `fd42:dead:beef:70::/64`, and the runtime delegated hostile GUA source-file return route on `p2p-nixos-router-core-nebula-nixos-router-upstream`. The cause was internal remote-prefix resolution using the virtual underlay-access edge added for overlay bootstrap path selection, then dropping the route because that virtual first hop has no real link. NFM now falls back to the real topology graph for internal remote-prefix routes when the selected first hop has no real link. Focused proof passes: `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-core-local-hostile-return-routes.sh`. Full proof passes: `NETWORK_REPO_DIRECT_TEST_OK=1 TEST_JOBS=4 bash tests/test.sh`. | reason=NFM owns forwarding-executable return routes. Virtual underlay-access edges are valid for overlay bootstrap/loopback path classification, but payload and DNS return-prefix routes must be emitted on real forwarding links for CPM/renderers to materialize without inference.

- state=solved | target=overlay-core-payload-fabric-golden-refresh | evidence=2026-05-25 after `network-labs` restored explicit `core-nebula <-> upstream` payload fabric links for `tri-site-s-router-overlay-egress`, full-loop NFM validation failed only the aggregate golden summaries: home changed from `12 25 10 25 3 1 58 570 573` to `12 26 11 26 3 1 60 627 630`, and lab changed from `11 18 9 18 2 1 44 409 416` to `11 19 10 19 2 1 46 441 448`; route summaries likewise gained the connected/default/internal routes expected from the restored payload leg. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-network-labs-output.sh` passes after refreshing `tests/expected/network-labs-{sites,routes}.tsv`. | reason=NFM owns the deterministic forwarding summary for real `network-labs` examples. The count increase is expected evidence of the restored explicit payload fabric adjacency, not a renderer recovery patch.

- state=solved | target=overlay-underlay-host-like-route-graph | evidence=2026-05-24 locked `s-router` rebuild advanced past compiler validation, then failed in NFM with `routing(loopbacks): unreachable router identity 'clab-router-core-nebula' from 'clab-router-access-client'` for the host-like core-nebula underlay attachment. NFM now adds overlay underlay tenant attachments as virtual route-graph edges only, so path search can classify loopback/static reachability without creating real `core-nebula <-> access-client` links or p2p interfaces; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-default-routes.sh` passes with the no-p2p host-like input. | reason=NFM owns route graph reachability over compiler semantics. A host-like overlay underlay attachment is forwarding-reachable for path selection, but it is not a topology link and must not be materialized as a p2p interface downstream.

- state=solved | target=service-source-uplink-lanes | evidence=2026-05-17 live `s-router-test` showed `nixos-router-policy` table `2004` for `iif downstream-mgmt` had no default route while nftables already allowed `downstream-mgmt` DNS to uplinks; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-service-source-uplink-lanes.sh` passes, and a dirty-tree compile of `s-router-test-three-site` emitted `nixos-router-access-mgmt` access-uplink lanes for `isp-a` and `isp-b`. | reason=Service-as-source relations are valid compiler contract data; NFM lane derivation was only matching tenant and tenant-set subjects, so service provider tenants did not receive the required access-uplink lane defaults.
- state=solved | target=model-explicit-nat66-source-contract | evidence=2026-05-23 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-sigma-simulated-isp-nat66-contract.sh` passes and proves NFM emits noisy source-scoped NAT66 intent only for simulated-ISP normal tenant lanes while excluding hostile ULA and routed Hetzner core paths. | reason=NFM now carries the platform-independent egress/NAT source-scope semantics CPM needs; CPM still owns turning that contract into concrete `natIntent` without inventing NAT66 from interface names or host facts.
- state=solved | target=model-dmz-dns-recursion-class | evidence=2026-05-23 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-node-placement.sh` passes and proves NFM emits explicit DNS service placement plus resolver preference order with access and non-WAN core resolvers ahead of WAN fallback cores. CPM `target=sigma-dmz-dns-internal-only-contract` still tracks the remaining public-recursion materialization bug. | reason=NFM preserves the semantic DNS handoff without renderer-side policy inference; CPM must consume it to output resolver contracts that distinguish internal-only DNS from public-recursive DNS.
- state=solved | target=p2p-specific-underlay-return-routes | evidence=2026-05-19 live Hetzner validation needed exact `10.80.0.10/31` return routes in policy tables so Nebula handshake replies selected the east-west DMZ lane instead of a broad `10.80.0.0/24` aggregate; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-p2p-specific-underlay-return-routes.sh` now checks `hetz-router-policy` and `hetz-router-downstream` emit the concrete p2p return routes for `s-router-test-three-site` and no longer emit the broad internal p2p aggregate. | reason=NFM owns p2p underlay forwarding truth; downstream CPM/renderers must not recover exact p2p lane routes from broad aggregates or local hotpatches.
- state=solved | target=access-tenant-gateway-host-addresses | evidence=2026-05-19 live Hetzner `hetz-router-access-dmz` had `tenant-dmz` on `10.90.10.0/24` while CPM/router validation expected DNS on `10.90.10.1`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-tenant-gateway-host-addresses.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test.sh` pass after tenant logical interfaces use host +1 addresses while preserving connected subnet routes. | reason=NFM owns logical tenant interface addressing; renderers must not correct invalid network-base interface addresses locally.
- state=solved | target=typed-service-egress-defaults | evidence=2026-05-20 fresh rsync from `s-router-test:/etc/network-artifacts` and `hetzner-nebula-prodtest-01:/etc/network-artifacts` proved live/rendered nft matched CPM, but `hetz-router-policy` returned `RTNETLINK answers: Network is unreachable` for `ip route get 1.1.1.1 from 10.90.10.1 iif downstream-dmz`; the synced CPM had `policy-derived-default` on `up-client-wan` but not `up-dmz-wan`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-service-source-uplink-lanes.sh` passed on 2026-05-20 and requires the DMZ access-uplink policy default for a DNS-typed service egress relation. | reason=NFM default-route eligibility incorrectly treated only `trafficType = "any"` egress as needing executable default reachability; typed allowed egress such as DMZ DNS still needs the lane route while firewall policy restricts ports.
- state=solved | target=policy-routing-source-prefix-contract | evidence=2026-05-21 `gron forwarding.json | rg 'tenantPrefixOwners|policy-derived-default|access-node-ipv6-prefix-esp-nixos-router-access-hostile'` showed NFM emits hostile static IPv4/ULA ownership, runtime source-file ownership, and the hostile east-west policy-derived defaults. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-source-scope-contract.sh` passes and checks both sides of that handoff. | reason=NFM owns the platform-independent source-prefix ownership plus lane default contract; CPM/renderers must preserve and materialize it instead of rediscovering tenant source scope from interface names.
- state=solved | target=overlay-core-loopback-underlay-access-return | evidence=2026-05-24 live `s-router-test` after locked full-loop showed `nixos-router-core-nebula ip route get 1.1.1.1` selected the modeled `access-client` underlay with source `10.19.0.8`, but IPv4 replies still failed. `gron /etc/network-artifacts/forwarding.json | rg 'nixos-router-upstream|p2p-nixos-router-core-nebula-nixos-router-upstream|10\\.19\\.0\\.8'` on `nixos-router-upstream` showed NFM emitted `internal-reachability` route `10.19.0.8/32 via 10.10.0.18` on the direct upstream/core-nebula p2p. That violates `transport.overlays[].underlayAccess = { kind = "tenant"; name = "client"; }`: overlay daemon underlay return traffic must resolve the overlay core runtime identity through the selected normal access path, not the direct overlay-core fabric adjacency. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-default-routes.sh` now fails if an upstream selector keeps an overlay core loopback route on the direct overlay-core p2p or if the route is missing from the access/uplink policy lane. | reason=NFM owns forwarding-executable route semantics. CPM and renderers must not delete a direct route after the fact; the forwarding model must make overlay-core loopback reachability follow the explicit underlay access path while preserving overlay payload reachability on the core adjacency.

- state=solved | target=overlay-underlay-access-default-route-self-loop | evidence=2026-05-23 full loop failed live with `nixos-router-core-nebula ip route get 1.1.1.1` selecting the selected access node, whose gateway immediately returned `Destination Net Unreachable`; `clab-hostile-node01` reached the core path and then died after the access/core hop. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-default-routes.sh` now fails without the fix and passes with the selected access node retaining WAN defaults toward downstream transit while still rejecting defaults back into the overlay core. | reason=The compiler can require an explicit underlay access tenant with WAN egress, but NFM owns route-collision and self-loop prevention: overlay-terminating cores must default toward the selected access node, and that access node must keep the normal access/downstream/policy/upstream default path instead of becoming a dead-end or self-loop.
- state=solved | target=overlay-access-uplink-defaults-without-core-default | evidence=2026-05-23 CPM inventory sweep failed after removing a CPM role-gated route synthesis: `s-router-overlay-dns-lane-policy` site-c emitted the access-uplink east-west p2p lane and `client -> external east-west` intent, but NFM emitted policy/upstream default-reachability only for `wan`. `bash tests/test-overlay-access-uplink-defaults-without-core-default.sh` now requires lane-specific IPv4/IPv6 defaults on policy and upstream-selector for the modeled east-west overlay lane. | reason=NFM owns executable forwarding consequences for allowed external overlay lanes. CPM must not invent `default-reachability` from roles such as policy/upstream-selector or from generated link names; it should only preserve and realize NFM route intent.
