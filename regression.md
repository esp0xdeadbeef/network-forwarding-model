- state=solved | target=overlay-core-payload-fabric-golden-refresh | evidence=2026-05-25 after `network-labs` restored explicit `core-nebula <-> upstream` payload fabric links for `tri-site-s-router-overlay-egress`, full-loop NFM validation failed only the aggregate golden summaries: home changed from `12 25 10 25 3 1 58 570 573` to `12 26 11 26 3 1 60 627 630`, and lab changed from `11 18 9 18 2 1 44 409 416` to `11 19 10 19 2 1 46 441 448`; route summaries likewise gained the connected/default/internal routes expected from the restored payload leg. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-network-labs-output.sh` passes after refreshing `tests/expected/network-labs-{sites,routes}.tsv`. | reason=NFM owns the deterministic forwarding summary for real `network-labs` examples. The count increase is expected evidence of the restored explicit payload fabric adjacency, not a renderer recovery patch.

- state=solved | target=overlay-underlay-host-like-route-graph | evidence=2026-05-24 locked `s-router` rebuild advanced past compiler validation, then failed in NFM with `routing(loopbacks): unreachable router identity 'clab-router-core-nebula' from 'clab-router-access-client'` for the host-like core-nebula underlay attachment. NFM now adds overlay underlay tenant attachments as virtual route-graph edges only, so path search can classify loopback/static reachability without creating real `core-nebula <-> access-client` links or p2p interfaces; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-default-routes.sh` passes with the no-p2p host-like input. | reason=NFM owns route graph reachability over compiler semantics. A host-like overlay underlay attachment is forwarding-reachable for path selection, but it is not a topology link and must not be materialized as a p2p interface downstream.

- state=solved | target=service-source-uplink-lanes | evidence=2026-05-17 live `s-router-test` showed `nixos-router-policy` table `2004` for `iif downstream-mgmt` had no default route while nftables already allowed `downstream-mgmt` DNS to uplinks; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-service-source-uplink-lanes.sh` passes, and a dirty-tree compile of `s-router-test-three-site` emitted `nixos-router-access-mgmt` access-uplink lanes for `isp-a` and `isp-b`. | reason=Service-as-source relations are valid compiler contract data; NFM lane derivation was only matching tenant and tenant-set subjects, so service provider tenants did not receive the required access-uplink lane defaults.
- state=solved | target=model-explicit-nat66-source-contract | evidence=2026-05-23 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-sigma-simulated-isp-nat66-contract.sh` passes and proves NFM emits noisy source-scoped NAT66 intent only for simulated-ISP normal tenant lanes while excluding hostile ULA and routed Hetzner core paths. | reason=NFM now carries the platform-independent egress/NAT source-scope semantics CPM needs; CPM still owns turning that contract into concrete `natIntent` without inventing NAT66 from interface names or host facts.
- state=solved | target=model-dmz-dns-recursion-class | evidence=2026-05-23 `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-dns-service-node-placement.sh` passes and proves NFM emits explicit DNS service placement plus resolver preference order with access and non-WAN core resolvers ahead of WAN fallback cores. CPM `target=sigma-dmz-dns-internal-only-contract` still tracks the remaining public-recursion materialization bug. | reason=NFM preserves the semantic DNS handoff without renderer-side policy inference; CPM must consume it to output resolver contracts that distinguish internal-only DNS from public-recursive DNS.
- state=solved | target=p2p-specific-underlay-return-routes | evidence=2026-05-19 live Hetzner validation needed exact `10.80.0.10/31` return routes in policy tables so Nebula handshake replies selected the east-west DMZ lane instead of a broad `10.80.0.0/24` aggregate; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-p2p-specific-underlay-return-routes.sh` now checks `hetz-router-policy` and `hetz-router-downstream` emit the concrete p2p return routes for `s-router-test-three-site` and no longer emit the broad internal p2p aggregate. | reason=NFM owns p2p underlay forwarding truth; downstream CPM/renderers must not recover exact p2p lane routes from broad aggregates or local hotpatches.
- state=solved | target=access-tenant-gateway-host-addresses | evidence=2026-05-19 live Hetzner `hetz-router-access-dmz` had `tenant-dmz` on `10.90.10.0/24` while CPM/router validation expected DNS on `10.90.10.1`; `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-access-tenant-gateway-host-addresses.sh` and `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test.sh` pass after tenant logical interfaces use host +1 addresses while preserving connected subnet routes. | reason=NFM owns logical tenant interface addressing; renderers must not correct invalid network-base interface addresses locally.
- state=solved | target=typed-service-egress-defaults | evidence=2026-05-20 fresh rsync from `s-router-test:/etc/network-artifacts` and `hetzner-nebula-prodtest-01:/etc/network-artifacts` proved live/rendered nft matched CPM, but `hetz-router-policy` returned `RTNETLINK answers: Network is unreachable` for `ip route get 1.1.1.1 from 10.90.10.1 iif downstream-dmz`; the synced CPM had `policy-derived-default` on `up-client-wan` but not `up-dmz-wan`. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-service-source-uplink-lanes.sh` passed on 2026-05-20 and requires the DMZ access-uplink policy default for a DNS-typed service egress relation. | reason=NFM default-route eligibility incorrectly treated only `trafficType = "any"` egress as needing executable default reachability; typed allowed egress such as DMZ DNS still needs the lane route while firewall policy restricts ports.
- state=solved | target=policy-routing-source-prefix-contract | evidence=2026-05-21 `gron forwarding.json | rg 'tenantPrefixOwners|policy-derived-default|access-node-ipv6-prefix-esp-nixos-router-access-hostile'` showed NFM emits hostile static IPv4/ULA ownership, runtime source-file ownership, and the hostile east-west policy-derived defaults. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-policy-source-scope-contract.sh` passes and checks both sides of that handoff. | reason=NFM owns the platform-independent source-prefix ownership plus lane default contract; CPM/renderers must preserve and materialize it instead of rediscovering tenant source scope from interface names.
- state=solved | target=overlay-core-loopback-underlay-access-return | evidence=2026-05-24 live `s-router-test` after locked full-loop showed `nixos-router-core-nebula ip route get 1.1.1.1` selected the modeled `access-client` underlay with source `10.19.0.8`, but IPv4 replies still failed. `gron /etc/network-artifacts/forwarding.json | rg 'nixos-router-upstream|p2p-nixos-router-core-nebula-nixos-router-upstream|10\\.19\\.0\\.8'` on `nixos-router-upstream` showed NFM emitted `internal-reachability` route `10.19.0.8/32 via 10.10.0.18` on the direct upstream/core-nebula p2p. That violates `transport.overlays[].underlayAccess = { kind = "tenant"; name = "client"; }`: overlay daemon underlay return traffic must resolve the overlay core runtime identity through the selected normal access path, not the direct overlay-core fabric adjacency. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-default-routes.sh` now fails if an upstream selector keeps an overlay core loopback route on the direct overlay-core p2p or if the route is missing from the access/uplink policy lane. | reason=NFM owns forwarding-executable route semantics. CPM and renderers must not delete a direct route after the fact; the forwarding model must make overlay-core loopback reachability follow the explicit underlay access path while preserving overlay payload reachability on the core adjacency.

- state=solved | target=overlay-underlay-access-default-route-self-loop | evidence=2026-05-23 full loop failed live with `nixos-router-core-nebula ip route get 1.1.1.1` selecting the selected access node, whose gateway immediately returned `Destination Net Unreachable`; `clab-hostile-node01` reached the core path and then died after the access/core hop. `NETWORK_REPO_DIRECT_TEST_OK=1 bash tests/test-overlay-underlay-access-default-routes.sh` now fails without the fix and passes with the selected access node retaining WAN defaults toward downstream transit while still rejecting defaults back into the overlay core. | reason=The compiler can require an explicit underlay access tenant with WAN egress, but NFM owns route-collision and self-loop prevention: overlay-terminating cores must default toward the selected access node, and that access node must keep the normal access/downstream/policy/upstream default path instead of becoming a dead-end or self-loop.
- state=solved | target=overlay-access-uplink-defaults-without-core-default | evidence=2026-05-23 CPM inventory sweep failed after removing a CPM role-gated route synthesis: `s-router-overlay-dns-lane-policy` site-c emitted the access-uplink east-west p2p lane and `client -> external east-west` intent, but NFM emitted policy/upstream default-reachability only for `wan`. `bash tests/test-overlay-access-uplink-defaults-without-core-default.sh` now requires lane-specific IPv4/IPv6 defaults on policy and upstream-selector for the modeled east-west overlay lane. | reason=NFM owns executable forwarding consequences for allowed external overlay lanes. CPM must not invent `default-reachability` from roles such as policy/upstream-selector or from generated link names; it should only preserve and realize NFM route intent.
